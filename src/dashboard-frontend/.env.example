# true(기본값)이면 실제 백엔드 없이 브라우저 내 mock 시뮬레이션으로 동작합니다.
# 실제 백엔드에 붙이려면 반드시 false로 명시해야 합니다.
VITE_USE_MOCK=true

# VITE_USE_MOCK=false 일 때 사용할 실제 백엔드 주소
VITE_API_BASE_URL=http://localhost:8000/api
VITE_WS_URL=ws://localhost:8000/ws
